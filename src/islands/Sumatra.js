import { createBlock } from "../utils/createBlock.js";

export function createSumatra() {
  const sumatraBlock = createBlock("Sumatra", 0x004d40); // Dark Green Floor

  // TODO: Add decorations for Sumatra here, relative to the block's center (0, H/2, 0).

  return sumatraBlock;
}
