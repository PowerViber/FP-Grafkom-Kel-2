import { createBlock } from "../utils/createBlock.js";

export function createJawa() {
  const jawaBlock = createBlock("Jawa", 0x1a237e);

  // TODO: Add decorations for Jawa here, relative to the block's center (0, H/2, 0).

  return jawaBlock;
}
